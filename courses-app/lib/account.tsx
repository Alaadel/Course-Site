export function purchaseCourse(courseId: number) {
    // In a real application, this would involve an API call to the backend to process the purchase
    console.log(`Course with ID ${courseId} has been purchased.`);
}